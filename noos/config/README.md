# config/

Configuração local e segredos (chave de API, se/quando a `AnthropicProvider` for ativada — PDF 03 §10, PDF 14 §5). **Nada aqui é commitado**: `config/*.cfg` está no `.gitignore` do projeto. Só este README fica versionado.
