# providers/

`base_provider.gd` (contrato) já existe. Implementações concretas chegam no **M4** (PDF 14, PDF 20): `ollama_provider.gd` (prioritário), `anthropic_provider.gd` (opt-in/futuro), `distilled_provider.gd` (Fase 3).
